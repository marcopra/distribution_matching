# @package _global_
# Pong Environment Configuration

env:
  name: "ALE/MarioBros-v5"
  render_mode: rgb_array
  atari:
    score_mask:
      enabled: true
  frameskip: 1

# agent:
#   feature_dim: 512
