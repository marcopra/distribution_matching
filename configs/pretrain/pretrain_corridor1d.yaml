# @package _global_
defaults:
  - /agent: rover_nystrom_pointmaze_debug
  - /env: continuous_rooms/continuous_corridor_1d 
  # - override hydra/launcher: submitit_local

# mode
reward_free: true

# task settings
domain: gym
task_name: ${env.name}
obs_type: pixels
frame_stack: 1
action_repeat: 1
discount: 0.9
resolution: 84
grayscale: false
random_init: true
random_goal: true
update_actor_after_critic_steps: 0

# train settings
num_train_frames: 2_000_000
num_seed_frames: 200

# eval
eval_every_frames: 10000
num_eval_episodes: 1

# snapshot
snapshots: [3000, 50000, 100000, 200000, 500000, 1000000, 2000000]
snapshot_dir: models/${obs_type}/${domain}/${agent.name}/${seed}

# replay buffer
replay_buffer_size: 1000000
replay_buffer_num_workers: 1
batch_size: ${agent.batch_size}
nstep: ${agent.nstep}
update_encoder: true
  
agent:
  feature_dim: 256
  lr_actor: 10
  pmd_steps: 50
  update_every_steps: 1
  pmd_eta_mode: backtracking
  svd_truncation: 2500
  compute_dtype: float32
  sink_schedule: 1
  debug_fixed_dataset_updates: true
  state_kernel_bandwidth_percentile: 5.0
  state_action_kernel_bandwidth_percentile: 5.0
  subsamples: null
  lambda_reg: 1e-2
  batch_size_actor: 200
  update_actor_every_steps: 1500
  T_init_steps: 1000


# misc
seed: 1
device: cuda
save_video: false
save_train_video: false
save_buffer: false
save_snapshot: false
use_tb: true
use_wandb: false
wandb_project: "pretrain_pointmaze"
wandb_entity: "urlb"
wandb_tag: none
wandb_run_name: ${now:%Y.%m.%d}_${now:%H%M}_${task_name}
wandb_id: none

# experiment
experiment: exp

hydra:
  run:
    dir: ./exp_local/${now:%Y.%m.%d}/${now:%H%M%S_%f}_${task_name}
  sweep:
    dir: ./exp_sweep/${now:%Y.%m.%d}/${now:%H%M}_${task_name}_${experiment}
    subdir: ${hydra.job.num}
  # launcher:
  #   timeout_min: 4300
  #   cpus_per_task: 10
  #   gpus_per_node: 1
  #   tasks_per_node: 1
  #   mem_gb: 160
  #   nodes: 1
  #   submitit_folder: ./exp_sweep/${now:%Y.%m.%d}/${now:%H%M}_${task_name}_${experiment}/.slurm
