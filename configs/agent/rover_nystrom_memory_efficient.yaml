# @package agent
_target_: agent.rover_nystrom_memory_efficient.RoverAgent
name: dist_matching
obs_type: ??? # to be specified later
obs_shape: ??? # to be specified later
grayscale: ${grayscale}
action_shape: ??? # to be specified later
lr_actor: 10 # eta
discount: ${discount} # gamma
lambda_reg: 1e-15
lr_T: 1e-4
batch_size: 1024
batch_size_actor: 10000
actor_fifo_size: 40000 # defaults to batch_size_actor - 1; first transition is kept separately
actor_fifo_batches_per_update: null # defaults to int(update_actor_every_steps / batch_size), minimum 1
subsamples: 4000
nystrom_subsample_source: fifo # [fifo, replay_buffer]
nstep: 1
lr_encoder: 1e-4
curl: false
embedding_sum_loss: 0.0
hidden_dim: 1024
feature_dim: null
T_init_steps: 1000
total_train_steps: ${num_train_frames}
update_every_steps: 100
update_actor_every_steps: 1500
pmd_steps: 100
pmd_eta_mode: backtracking # [none, adagrad, backtracking]
pmd_best_iterate: true
pmd_grad_clip_norm: 0.0
pmd_adagrad_eps: 1e-8
pmd_eta_min: 1e-8
pmd_eta_max: 1e8
pmd_backtrack_factor: 0.25
pmd_backtrack_max_trials: 12
sink_schedule: "linear(0.0, 0.005, 100_000)"
epsilon_schedule: 0.15
mode: l1 # [l1, l2]
reward: false # whether to learn a reward predictor or not
embeddings: true  # whether to use embeddings or not, if obs_type is pixels this is always true
use_tb: ${use_tb}
use_wandb: ${use_wandb}
num_expl_steps: ??? # to be specified later
device: ${device}
compute_dtype: float64 # [float32, float64]
